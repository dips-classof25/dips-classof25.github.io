<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Class Time Capsule</title>
    <link rel="stylesheet" href="../styles/styles.css">

	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Handlee&family=Indie+Flower&family=Instrument+Serif:ital@0;1&display=swap" rel="stylesheet">
	
	<style>
		:root {
			--theme-light: <%= theme[0] %>;
			--theme-dark: <%= theme[1] %>;
		}
	</style>
</head>
<body>
	
    <main>
			
	    <div class="time-capsule-container">
	        <header class="capsule-header">
	            <h1>Remembering 12 <%= section %>.</h1>
	            <p>notes and memories from friends and classmates.</p>
	        </header>

	        <section class="messages-section">
			  <% messages.each do |message| %>		
				<% if message[:content].length < 150 %>
				<div class="post-it" style="transform: rotate(<%= message[:rot] %>deg);">
				<% elsif message[:content].length < 250 %>
				<div class="taped" style="transform: rotate(<%= message[:rot] %>deg);">
				<% else %>
				<div class="card-type">
				<% end %>
			    
			      <p><%= message[:content] %></p>
			      <span class="date"><%= message[:name] %></span>
			    </div>
			  <% end %>
	        </section>
		   
		   <section class="links">
			   <a href="https://open.spotify.com/playlist/<%= spotify_ids[section] %>" class="s">Spotify</a>
			   <a href="https://drive.google.com/drive/folders/<%= folder_ids[section] %>" class="p">Photo Gallery</a>
		   </section>
		   
		   <section class="end-note">

  		   Here's to the class of 2025.<br>
		   <img src="../images/1.gif" height="25px">
		   <img src="../images/2.gif" height="25px">
		   </section>
	    </div>
</body>
</html>
